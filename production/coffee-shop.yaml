kind: Service
apiVersion: v1
metadata:
  name: coffee-shop-sec
spec:
  selector:
    app: coffee-shop-sec
  ports:
  - port: 8080
---

kind: Deployment
apiVersion: apps/v1
metadata:
  name: coffee-shop-sec
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coffee-shop-sec
      version: v1
  template:
    metadata:
      labels:
        app: coffee-shop-sec
        version: v1
      annotations:
        revision: "13c2375c6432337a595a72d1a8442b80072b3320"
    spec:
      containers:
      - name: coffee-shop-sec
        image: registry.hub.docker.com/ajaygupta1999/coffee-shop:13c2375c6432337a595a72d1a8442b80072b3320
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        readinessProbe:
          exec:
            command: ["sh", "-c", "curl -s http://localhost:8080/q/health | grep -q coffee-shop"]
          initialDelaySeconds: 15
      imagePullSecrets:
      - name: regsecret
      restartPolicy: Always
---

